apiVersion: v1
kind: Service

metadata:
  name: devops-task-service

spec:
  type: NodePort

  selector:
    app: devops-task-app

  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
